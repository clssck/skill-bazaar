# Migration artifacts generated by migrate/scripts/.
# Patterns here apply only inside snowflake-postgres/, so a `*.dump`
# (e.g. a core dump or test fixture) elsewhere in the workspace
# isn't silently hidden.
*.dump
*.backup
assessment_data.json
migration_assessment_report.html
